# TODO
- [ ] Implement requested global CSS in `src/index.css` for Hero/Navbar/Header/MainLayout/Home.
- [ ] Ensure no selector clashes: keep Tailwind/@theme intact; scope/rename any duplicate selectors if needed.
- [x] Verify selectors match component className/id values in existing JSX.
- [x] Run dev server/lint (if available) to ensure CSS builds without errors.



